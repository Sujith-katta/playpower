services:
  # ----------------------------------------------------
  # Single Unified Web Service (Spring Boot + Embedded Next.js)
  # ----------------------------------------------------
  - type: web
    name: airbnb-clone
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free
    region: oregon
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
